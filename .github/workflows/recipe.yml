name: 🥘 Weekly Recipe Scraper

on:
  schedule:
    - cron: "0 12 * * 1"  # Every Monday at 12:00 UTC
  workflow_dispatch:      # Allow manual runs

permissions:
  contents: write

jobs:
  scrape:
    runs-on: ubuntu-latest

    steps:
      - name: 📦 Checkout repository
        uses: actions/checkout@v4

      - name: 🐍 Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: 📚 Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests beautifulsoup4

      - name: 🍳 Run weekly scraper
        run: python scripts/scraper.py

      - name: 🧹 Run layout fixer
        run: python scripts/fix_recipe_layouts.py

      - name: 🆕 Update Recently Added on homepage
        run: python scripts/update_recent_recipes.py



      - name: 🕵️ Detect changes
        id: detect
        run: |
          git config user.name "Recipe Bot"
          git config user.email "actions@github.com"
          git add recipes/weekly_recipes.md
          if ! git diff --cached --quiet; then
            echo "changed=true" >> $GITHUB_ENV
          fi

      - name: 🧾 Commit and push updates
        if: env.changed == 'true'
        run: |
          DATE=$(date +"%b %d, %Y")
          COUNT=$(grep -o "<li>" recipes/weekly_recipes.md | wc -l || echo 0)
          git commit -m "🍳 Weekly update — ${COUNT} recipes total (${DATE})"
          git push
